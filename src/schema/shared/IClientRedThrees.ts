import { IClientCardStack } from "./IClientCardStack.js";

export interface IClientRedThrees extends IClientCardStack {
	// extends IClientCardStack with cards array
	// each card in the red threes stack
}
