import { IClientLobbyPlayer } from "@/schema/shared/IClientLobbyPlayer";
import { Server, Socket } from "socket.io";

export function onPlayerReady(socket:Socket, io:Server, props:IClientLobbyPlayer ) {
    
}